# 코드베이스 설명

## 1. 저장소 개요

이 저장소는 소프트웨어공학 수업에서 Git과 GitHub를 이용한 팀 협업 과정을 실습하기 위한 팀 소개 웹페이지이다. 각 팀은 자신의 팀 이름과 모토를 작성하고, 팀원들은 각자 브랜치에서 자기소개를 추가한 후 Pull Request를 통해 팀 저장소에 반영한다.

웹페이지는 별도의 서버나 프로그램 설치 없이 HTML, CSS, JavaScript로 실행되는 정적 웹페이지로 구성되어 있다.

## 2. 파일 및 폴더 구성

### index.html

웹페이지의 기본 구조를 만드는 파일이다. 페이지 제목, 팀 검색창, 팀 소개 카드가 표시되는 공간과 하단 안내 영역이 포함되어 있다. 또한 `style.css`, `data/teams.js`, `app.js`를 불러온다.

### style.css

웹페이지의 디자인을 담당하는 파일이다. 글꼴, 색상, 검색창, 팀 카드의 모양과 배치 등을 설정한다.

### app.js

`data/teams.js`에 저장된 팀 정보를 읽어 웹페이지에 팀 소개 카드를 표시하는 파일이다. 팀 이름이나 팀원 이름을 검색하는 기능과 현재 표시된 팀 및 팀원 수를 계산하는 기능도 처리한다.

### data/teams.js

각 팀의 정보를 저장하는 파일이다. 팀 이름, 팀 모토, 팀원의 이름, GitHub ID, 역할과 인사말이 들어 있다. 각 팀은 이 파일에서 자신에게 배정된 팀 블록만 수정해야 한다.

### README.md

저장소의 목적, 웹페이지 실행 방법과 팀별 Git 및 Pull Request 실습 순서를 설명하는 안내 문서이다.

### .github/pull_request_template.md

Pull Request를 작성할 때 사용하는 기본 양식이다. 무엇을 수정했는지, 왜 수정했는지, 어떻게 확인했는지를 작성할 수 있도록 구성되어 있다.

## 3. 파일의 연결 관계

`index.html`은 먼저 `data/teams.js`를 불러와 팀 정보를 준비한 다음 `app.js`를 실행한다. `app.js`는 준비된 팀 정보를 읽어서 팀 소개 카드를 만들고, 이를 `index.html`의 팀 목록 영역에 표시한다. `style.css`는 이렇게 표시된 웹페이지와 팀 카드의 디자인을 담당한다.

전체적인 실행 흐름은 다음과 같다.

1. `data/teams.js`에서 팀 정보를 불러온다.
2. `app.js`가 팀 정보를 이용해 팀 카드를 만든다.
3. 생성된 팀 카드가 `index.html`에 표시된다.
4. `style.css`가 웹페이지의 디자인을 적용한다.

## 4. 실행 방법

별도의 설치나 빌드 과정은 필요하지 않다. 저장소 폴더에 있는 `index.html` 파일을 웹 브라우저로 열면 웹페이지가 실행된다.

실행 후 팀 이름, 모토와 팀원 정보가 정상적으로 표시되는지 확인한다. 검색창에 팀 이름이나 팀원 이름을 입력하여 검색 기능이 작동하는지도 확인할 수 있다.

## 5. 수정 시 주의사항

`data/teams.js`를 수정할 때는 배정받은 자기 팀 블록만 수정해야 한다. 다른 팀의 정보를 수정하면 안 되며, 비밀번호나 전화번호와 같은 개인정보도 입력하지 않아야 한다.

또한 괄호, 따옴표와 쉼표가 빠지면 웹페이지가 정상적으로 실행되지 않을 수 있으므로 기존 작성 형식을 유지해야 한다. GitHub ID에는 전체 주소가 아니라 아이디만 입력한다.